/**
 * Button CustomId enums for Discord button interactions
 */

/**
 * Dashboard control button IDs
 */
export enum DashboardButtonId {
    PlayPause = 'dashboard-play-pause',
    Skip = 'dashboard-skip',
    Stop = 'dashboard-stop',
    Loop = 'dashboard-loop',
    Shuffle = 'dashboard-shuffle',
    LoopSelect = 'dashboard-loop-select',
}

/**
 * Queue list control button IDs
 */
export enum QueueButtonId {
    Previous = 'queue-prev',
    Next = 'queue-next',
    Delete = 'queue-delete',
    Clear = 'queue-clear',
}

/**
 * Music save button IDs
 */
export enum MusicButtonId {
    Save = 'music-save',
}

/**
 * Volume control button IDs
 */
export enum VolumeButtonId {
    Volume25 = 'volume-25',
    Volume50 = 'volume-50',
    Volume75 = 'volume-75',
    Volume100 = 'volume-100',
}
