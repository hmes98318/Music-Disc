import { status } from "./status";
import { queue } from "./queue";
import { removeList, removeTrack } from "./remove";


const embeds = {
    status,
    queue,
    removeList,
    removeTrack
}

export { embeds };